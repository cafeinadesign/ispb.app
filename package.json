{
  "name": "ispb.app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Catálogo amigável de participantes Pix/SPI e oportunidades no ecossistema financeiro brasileiro.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsx scripts/ssg.ts && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "validate:data": "tsx scripts/validate-data.ts"
  },
  "dependencies": {
    "@thiagoprazeres/ispb-participants": "^1.2.0",
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.10"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0"
  }
}
